1) Run "sh build.sh" to build a code and push the image
2) Run "ansbile-playbook kubedeployement.yaml" for Kubernetes cluster set-up
3) Run "sh helloworld.sh" to deploy application to Kubernetes cluster
